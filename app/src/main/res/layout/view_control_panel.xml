<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="220dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/bg_panel_card"
    android:elevation="8dp">

    <TextView
        android:id="@+id/tvPanelTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/panel_title"
        android:textColor="@color/text_primary"
        android:textStyle="bold"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/etAuthorId"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:hint="@string/author_id_hint"
        android:inputType="text"
        android:maxLines="1"
        android:padding="10dp"
        android:background="@drawable/bg_panel_input"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/text_assist"
        android:textSize="13sp" />

    <Button
        android:id="@+id/btnStartCheck"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="8dp"
        android:backgroundTint="@color/brand_primary"
        android:textColor="@color/white"
        android:text="@string/panel_detect" />

    <Button
        android:id="@+id/btnOpenSettings"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginTop="8dp"
        android:backgroundTint="@color/secondary_button_bg"
        android:text="@string/panel_settings"
        android:textColor="@color/text_primary" />
</LinearLayout>
